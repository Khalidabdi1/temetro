name: Build & Release Electron App (Linux All Formats)

on:
  push:
    tags:
      - 'v*.*.*' 

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y rpm libarchive-tools zstd

      - name: Build Source Code
        run: npm run build

      - name: Clean electron-builder cache
        run: rm -rf ~/.cache/electron-builder

      - name: Build and Publish Electron App
        run: npx electron-builder --linux AppImage deb rpm pacman --publish always
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: List release folder
        run: ls -lh dist